/**
 * Placeholder for the real backend client. Not wired up yet — there is no
 * backend for this prototype. When one exists:
 *
 * 1. Implement categoryService/listingService/etc. here with the same
 *    method signatures as services/mock/*, backed by fetch() calls.
 * 2. Flip USE_MOCKS in services/index.ts to false (or drive it from
 *    config/environment.ts once that exists).
 *
 * Keeping the method signatures identical to the mock services means
 * screens never need to change when the switch happens.
 */
export {};
